gnome-extension-developer/

# Build artifacts: regenerated by `make schemas` and `make pack`.
src/schemas/gschemas.compiled
*.shell-extension.zip

# Analyzer virtualenv, built on demand by `make shexli`.
.shexli-venv/